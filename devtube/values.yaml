# # Default values for devtube.
# # This is a YAML-formatted file.
# # Declare variables to be passed into your templates.



# image:
#   repository: nginx
#   pullPolicy: IfNotPresent
#   # Overrides the image tag whose default is the chart appVersion.
#   tag: ""
# imagePullSecrets: []
# nameOverride: ""
# fullnameOverride: ""
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""
# podAnnotations: {}
# podSecurityContext: {}
# # fsGroup: 2000

# securityContext: {}
# # capabilities:
# #   drop:
# #   - ALL
# # readOnlyRootFilesystem: true
# # runAsNonRoot: true
# # runAsUser: 1000
service:
  type: ClusterIP
  port: 80
# autoscaling:
#   enabled: false
#   minReplicas: 1
#   maxReplicas: 100
#   targetCPUUtilizationPercentage: 80
#   # targetMemoryUtilizationPercentage: 80
# nodeSelector: {}
# tolerations: []
# affinity: {}
namespace: devtube
ingress:
  enabled: true
  certificate:
    enabled: false
    email: ronsh012@gmail.com
    host: ronsharabi.ddns.net
mongodb:
  auth:
    rootUser: "Username"
    rootPassword: "Password"
  architecture: replicaset
  useStatefulSet: true
  replicaCount: 3
api:
  ingress:
    enabled: true
  image: devtube.azurecr.io/devtube-api
  image_tag: 1.0.3
  jwt_key: "example"
  port: 5000
  resources:
    requests:
      memory: 64Mi
      cpu: 250m
    limits:
      memory: 128Mi
      cpu: 500m
  autoscaling:
    enabled: false
    replicaCount: 1
client:
  ingress:
    enabled: true
  image: devtube.azurecr.io/devtube-app
  image_tag: 1.0.2
  port: 3000
  resources:
    requests:
      memory: 64Mi
      cpu: 250m
    limits:
      memory: 128Mi
      cpu: 500m
  autoscaling:
    enabled: false
    replicaCount: 1
